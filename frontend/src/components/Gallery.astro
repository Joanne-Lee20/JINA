---
const images = [
  { src: "/logosketches.jpeg", alt: "Image 1" },
  { src: "/minigames.jpeg", alt: "Image 2" },
  { src: "/sketches.jpeg", alt: "Image 3" },
];
---

<section class="gallery">
  {images.map(({ src, alt }) => <img src={src} alt={alt} loading="lazy" />)}
</section>

<style>
  .gallery {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1.5rem;
    padding: 2rem;
  }

  .gallery img {
    width: 100%;
    height: 400px;
    border-radius: 0.5rem;
    object-fit: cover;
    transition: transform 0.3s ease;
    cursor: pointer;
  }

  .gallery img:hover {
    transform: scale(1.03);
  }
</style>
